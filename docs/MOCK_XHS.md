# Mock XHS 链路

这是一个仅用于本地集成测试的 XHS-like 服务，不会访问真实小红书，也不包含真实平台签名或设备指纹。

启动：

```bash
npm run mock:xhs
```

启动后会打印示例短链，例如 `http://127.0.0.1:随机端口/s/demo123`。客户端会跟随 302 跳转，提取 `demo123`，使用 Mock HMAC 签名请求详情接口，并从多档视频流中选择最高档地址。

测试覆盖有效签名、短链跳转、最高画质选择和未签名请求被拒绝。测试密钥固定为测试用途，不能用于真实平台。
