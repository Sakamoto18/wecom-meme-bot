{
  "api_url": {
    "description": "QQ Bot HTTP 接口",
    "type": "string",
    "default": "http://qq-bot:8787/v1/qq/message",
    "hint": "使用本仓库 Compose 部署时无需修改"
  },
  "api_token": {
    "description": "QQ Bot API 令牌",
    "type": "string",
    "default": "",
    "hint": "Compose 部署会通过 LONGTU_QQ_API_TOKEN 自动注入",
    "obvious_hint": true
  },
  "allowed_groups": {
    "description": "允许使用机器人的 QQ 群",
    "type": "string",
    "default": "",
    "hint": "英文逗号分隔；留空允许所有群。也可设置 LONGTU_QQ_ALLOWED_GROUPS"
  },
  "reply_only_when_waked": {
    "description": "群聊仅在被 @ 或命中 AstrBot 唤醒词时回复",
    "type": "bool",
    "default": true
  },
  "reply_only_when_at": {
    "description": "群聊必须明确 @机器人 才回复",
    "type": "bool",
    "default": true,
    "hint": "推荐开启；开启后普通唤醒词不会触发，优先级高于“仅被唤醒时回复”"
  },
  "observe_group_messages": {
    "description": "静默记录允许群中的普通消息，用于成员身份和群聊语境记忆",
    "type": "bool",
    "default": true,
    "hint": "不会回复，但会停止 AstrBot 后续默认 LLM，避免普通消息被重复处理；可用 LONGTU_QQ_OBSERVE_GROUP_MESSAGES 覆盖"
  },
  "ignored_wake_commands": {
    "description": "不由龙图服务回复的旧 AstrBot 唤醒命令",
    "type": "string",
    "default": "/w",
    "hint": "英文逗号分隔；专用 Bot 模式下所有非白名单斜杠命令仍会被 Bridge 静默拦截，不会进入默认模型"
  },
  "ignore_slash_commands": {
    "description": "群聊中忽略未列出的斜杠指令",
    "type": "bool",
    "default": true,
    "hint": "兼容旧配置项；Bridge 只放行图库管理 /add、/tag、/del 与超管 /stop，/w、/help 及其他插件命令都会被直接停止"
  },
  "send_processing_hint": {
    "description": "生成回答前发送处理提示",
    "type": "bool",
    "default": false
  },
  "media_ack_emoji_id": {
    "description": "视频分享解析前挂在原消息上的 QQ 表情回应 ID",
    "type": "string",
    "default": "128077",
    "hint": "默认是点赞；NapCat 使用 set_msg_emoji_like，QQ_MEDIA_ACK_EMOJI_ID 可覆盖"
  },
  "media_excluded_groups": {
    "description": "禁止视频分享解析的 QQ 群号",
    "type": "string",
    "default": "",
    "hint": "英文逗号分隔；也可设置 QQ_MEDIA_EXCLUDED_GROUPS"
  },
  "request_timeout_seconds": {
    "description": "等待 QQ Bot 服务的最长秒数",
    "type": "int",
    "default": 190
  },
  "daily_usage_report_enabled": {
    "description": "每天私聊日报收件人推送上一自然日的群聊用量日报",
    "type": "bool",
    "default": true,
    "hint": "收件人来自 LONGTU_QQ_USAGE_REPORT_USERS，可配置多个 QQ 且不获得超管权限；超管可用 /usage-report YYYY-MM-DD 回捞指定日期并私聊自己；时区固定为 Asia/Shanghai"
  },
  "daily_usage_report_hour": {
    "description": "用量日报发送小时（北京时间）",
    "type": "int",
    "default": 9
  },
  "daily_usage_report_minute": {
    "description": "用量日报发送分钟（北京时间）",
    "type": "int",
    "default": 0
  },
  "error_message": {
    "description": "后端不可用时回复的提示",
    "type": "string",
    "default": "龙图服务暂时不可用，请稍后再试。"
  }
}
